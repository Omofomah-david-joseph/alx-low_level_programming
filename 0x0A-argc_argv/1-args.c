#include "main.h"
#include <stdio.h>

/**
 * main - prints number of arguments in the programm
 * @argc: number of arguments
 * @argv: array of arguments
 *
 * Return: Always 0 (success)
 */
int main(int argc, char *argv[])
{
	(void) argv; /* do ingnore argv*/
	printf("%d\n"' argc - 1);

	return (0);
}
