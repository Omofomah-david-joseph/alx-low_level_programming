#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: first interger value
 * @b: second value
 * Return: Always 0 (success)
 */
int mul(int a, int b)
{
	int mult;

	mult = a * b;

	return (mult);
}
